#include "..\..\script_macros.hpp"
/*
	Thieves

	Thieves-Gaming.de

	Diese Mission ist bearbeitet von dem Development Team von Thieves-Gaming.de! 
	Diese Mission darf von keinem anderen genutzt werden außer von Thieves-Gaming.de!
*/	
	
	if (husky_coplevel == 1) then {
		["Sie sind Polizeimeisteranwärter","Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 2) then {
		["Sie sind Polizeimeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 3) then {
		["Sie sind Polizeiobermeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 4) then {
		["Sie sind Polizeihauptmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 5) then {
		["Sie sind Polizeikommissarsanwärter", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 6) then {
		["Sie sind Polizeikommissar", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 7) then {
		["Sie sind Polizeioberkommissar", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 8) then {
		["Sie sind Polizeihauptkommissar", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 9) then {
		["Sie sind Erster Polizeihauptkommissar Anwärter", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 10) then {
		["Sie sind Erster Polizeihauptkommissar", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 11) then {
		["Sie sind Polizeirat", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 12) then {
		["Sie sind Polizei Ausbilder Anwärter", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 13) then {
		["Sie sind Polizei Ausbilder", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 14) then {
		["Sie sind Stv. Polizeidirektor", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 15) then {
		["Sie sind Leitender Polizeidirektor", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_coplevel == 16) then {
		["Sie sind Administrator", "Hinweis"] spawn husky_fnc_createNotification;
	};
	
	if (husky_mediclevel == 1) then {
		["Sie sind Praktikant", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 2) then {
		["Sie sind Feuerwehrmannanw�rter", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 3) then {
		["Sie sind Feuerwehrmann", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 4) then {
		["Sie sind Oberfeuerwehrmann", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 5) then {
		["Sie sind Hauptfeuerwehrmann", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 6) then {
		["Sie sind L�schmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 7) then {
		["Sie sind Oberl�schmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 8) then {
		["Sie sind Hauptl�schmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 9) then {
		["Sie sind Brandmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 10) then {
		["Sie sind Oberbrandmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 11) then {
		["Sie sind Hauptbrandmeister", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 12) then {
		["Sie sind Brandrat", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 13) then {
		["Sie sind Feuerwehr Ausbilder", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 14) then {
		["Sie sind Co. Branddirektor ", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 15) then {
		["Sie sind Branddirektor", "Hinweis"] spawn husky_fnc_createNotification;
	};
	if (husky_mediclevel == 16) then {
		["Sie sind Administrator", "Hinweis"] spawn husky_fnc_createNotification;
	};